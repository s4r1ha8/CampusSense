{
  "name": "campus-temp-backend",
  "version": "1.0.0",
  "description": "Serverless Lambda backend for CampusSense",
  "type": "module",
  "scripts": {
    "build":     "tsc",
    "typecheck": "tsc --noEmit",
    "test":      "vitest run",
    "test:watch":"vitest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-sns":      "^3.600.0",
    "@aws-sdk/util-dynamodb":   "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node":       "^22.0.0",
    "typescript":        "^5.5.0",
    "vitest":            "^1.6.0"
  }
}
